In Java Data Structures (DSA), a Tree is a hierarchical data structure made up of nodes connected by edges.

Basic Tree Terminology
        A
      /   \
     B     C
    / \   / \
   D   E F   G


Root → Top node (A)
Parent → Node with children (A is parent of B and C)
Child → Node below a parent (B is child of A)
Leaf Node → Node with no children (D, E, F, G)
Edge → Connection between nodes
Height → Longest path from root to a leaf
Depth → Distance from root to a node


Types of Trees
1. Binary Tree

Each node has at most 2 children.

      10
     /  \
    5   20
2. Binary Search Tree (BST)
Left child < Parent
Right child > Parent
       50
      /  \
    30    70
   / \    / \
 20 40  60 80

Operations:

Search → O(log n) average
Insert → O(log n) average
Delete → O(log n) average


3. AVL Tree

A self-balancing BST.

Height difference between left and right subtree is at most 1.
Uses rotations (Left, Right, Left-Right, Right-Left).


4. Heap

A complete binary tree.

Max Heap

Parent ≥ Children
      50
     /  \
   30    20

Min Heap

Parent ≤ Children

Uses:

Priority Queue
Heap Sort


5. B-Tree
Used in databases and file systems.
A node can have many children.


6. Trie

Stores strings efficiently.

      Root
      |
      c
      |
      a
     / \
    r   t

Used for:

Dictionary
Auto-complete
Spell checking
Tree Traversals
Inorder (Left → Root → Right)
    2
   / \
  1   3

Output: 1 2 3
Preorder (Root → Left → Right)

Output:

2 1 3
Postorder (Left → Right → Root)

Output:

1 3 2
Level Order (Breadth-First)

Output:

2 1 3

Uses a Queue.

Java Binary Tree Example
class Node {
    int data;
    Node left, right;

    Node(int data) {
        this.data = data;
        left = right = null;
    }
}

public class BinaryTree {
    public static void inorder(Node root) {
        if (root == null)
            return;

        inorder(root.left);
        System.out.print(root.data + " ");
        inorder(root.right);
    }

    public static void main(String[] args) {
        Node root = new Node(10);
        root.left = new Node(5);
        root.right = new Node(20);

        inorder(root);
    }
}

Output:

5 10 20
Time Complexities
Operation	BST (Average)	BST (Worst)
Search	O(log n)	O(n)
Insert	O(log n)	O(n)
Delete	O(log n)	O(n)
Traversal	O(n)	O(n)